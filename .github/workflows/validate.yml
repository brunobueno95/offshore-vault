name: Validate vault

# PLACEHOLDER WORKFLOW
# This file will run the validator script on every push and pull request.
# The validator script itself is not yet implemented (Phase 4 work).
# Once tools/validate.py exists, uncomment the job below.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Validator not yet implemented. This job will call python tools/validate.py offshore-vault/ once the validator exists."

  # Uncomment when validator is built:
  #
  # validate-real:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"
  #     - name: Install dependencies
  #       run: pip install pyyaml
  #     - name: Run validator
  #       run: python tools/validate.py offshore-vault/
  #     - name: Upload validator report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: validation-report
  #         path: |
  #           offshore-vault/_VALIDATION_REPORT.md
  #           offshore-vault/_UNRESOLVED_LINKS.md
  #           offshore-vault/_TODO_QUEUE.md
